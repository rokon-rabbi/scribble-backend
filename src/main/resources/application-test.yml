spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/scribble
    username: scribble
    password: scribble

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop

  data:
    redis:
      host: localhost
      port: 6379
