spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/scribble
    username: scribble
    password: scribble

  flyway:
    enabled: true
    clean-disabled: false

  jpa:
    hibernate:
      ddl-auto: validate

  data:
    redis:
      host: localhost
      port: 6380
